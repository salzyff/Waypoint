import {useEffect,useState} from 'react';
import {NavLink,Outlet,Link,useNavigate} from 'react-router-dom';
import {LayoutGrid,Package,Terminal,Boxes,Globe2,Folder,BookOpen,Building2,Settings,Layers,Link2,History,Search,Bell,ChevronsUpDown,ArrowUpRight,Command,Menu,X,Check} from 'lucide-react';
import {useStore} from '../app/store';
import {Modal,Button} from './ui';

const links=[['/dashboard','Overview',LayoutGrid],['/shipments','Shipments',Package],['/compile','Shipment compiler',Terminal],['/products','Products',Boxes],['/compare','Compare markets',Globe2],['/documents','Document vault',Folder],['/rules','Rule explorer',BookOpen]] as const;
export function Logo(){return <Link to="/" className="logo" aria-label="WAYPOINT home"><span className="logo-mark" aria-hidden="true"><Globe2 size={23} strokeWidth={2.4}/></span>WAYPOINT</Link>}

export default function Shell(){
 const {state,role,toast,orgs,switchOrg,orgId,update,user}=useStore();
 const [search,setSearch]=useState(false),[q,setQ]=useState(''),[bell,setBell]=useState(false),[mobile,setMobile]=useState(false);const navigate=useNavigate();
 useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setSearch(x=>!x)}};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[]);
 const results=[...state.shipments.map(s=>({name:s.name+' · '+s.id,to:'/compile/'+s.id,type:'Shipment'})),...state.products.map(p=>({name:p.name,to:'/products',type:'Product'})),...state.documents.map(d=>({name:d.name,to:'/documents',type:'Document'})),...state.packs.flatMap(p=>p.rules.map(r=>({name:r.title,to:'/rules',type:'Rule'}))),...state.sources.map(s=>({name:s.title,to:'/admin/sources',type:'Source'}))].filter(r=>r.name.toLowerCase().includes(q.toLowerCase())).slice(0,10);
 const nav=(to:string,label:string,Icon:typeof LayoutGrid)=><NavLink key={label} to={to} className={({isActive})=>'nav-item '+(isActive?'active':'')} onClick={()=>setMobile(false)}><Icon size={18}/>{label}{label==='Shipments'&&<span className="nav-count">{state.shipments.length}</span>}</NavLink>;
 return <div className="app-layout"><aside className={'sidebar '+(mobile?'mobile-open':'')}><div className="sidebar-brand"><Logo/><button className="mobile-only icon-btn" onClick={()=>setMobile(false)} aria-label="Close navigation"><X/></button></div><div className="org-switch"><span className="avatar company">{state.organisation.name.slice(0,2).toUpperCase()||'OR'}</span><span><strong>{state.organisation.name||'Your organisation'}</strong><small>Organisation workspace</small></span><ChevronsUpDown size={15}/></div>{orgs.length>1&&<select aria-label="Organisation" value={orgId} onChange={e=>switchOrg(e.target.value)}>{orgs.map(o=><option key={o.organisation_id} value={o.organisation_id}>{o.organisations.name}</option>)}</select>}<div className="nav-label">WORKSPACE</div><nav>{links.map(([to,label,Icon])=>nav(to,label,Icon))}</nav><div className="nav-label second">MANAGE</div>{nav('/organisation','Organisation',Building2)}{nav('/settings','Settings',Settings)}{role==='platform_admin'&&<><div className="nav-label second">PLATFORM</div>{nav('/admin/rule-packs','Rule packs',Layers)}{nav('/admin/sources','Sources',Link2)}{nav('/admin/audit','Audit logs',History)}</>}<div className="sidebar-bottom"><div className="compiler-promo"><span className="mini-label">BUILT FOR WHAT’S NEXT</span><strong>Less border friction.<br/>More business forward.</strong><Link to="/shipments/new">Compile a shipment <ArrowUpRight size={16}/></Link></div><Link className="user-row" to="/settings"><span className="avatar">{(user.split('@')[0]||'YO').slice(0,2).toUpperCase()}</span><span><strong>{user||'Workspace user'}</strong><small>{role.replace('_',' ')}</small></span><ChevronsUpDown size={15}/></Link></div></aside><div className="app-main"><header className="topbar"><div className="breadcrumb"><button className="mobile-only icon-btn" onClick={()=>setMobile(true)} aria-label="Open navigation"><Menu/></button><span>Workspace</span><span>/</span><strong>{state.organisation.name||'Your organisation'}</strong></div><div className="top-actions"><button className="search-trigger" onClick={()=>setSearch(true)}><Search size={16}/><span>Search anything</span><kbd>⌘ K</kbd></button><button className="icon-btn notification-btn" aria-label="Notifications" onClick={()=>setBell(true)}><Bell size={19}/>{state.notifications.some(n=>!n.read)&&<i/>}</button></div></header><main className="workspace"><Outlet/></main><footer className="app-footer"><span>WAYPOINT · Compile before you ship.</span><span className="system-online"><i/>Connected workspace</span></footer></div>{toast&&<div className="toast" role="status"><Check size={18}/>{toast}</div>}<Modal open={search} onClose={()=>setSearch(false)} title="Search & commands"><div className="command-input"><Search/><input autoFocus placeholder="Search shipments, products, rules…" value={q} onChange={e=>setQ(e.target.value)}/></div>{!q&&<div className="commands">{[['New shipment','/shipments/new'],['Compile shipment','/compile'],['Compare markets','/compare'],['Upload document','/documents'],['Create product','/products'],['Open rule explorer','/rules']].map(([name,to])=><button key={name} onClick={()=>{navigate(to);setSearch(false)}}><Command size={16}/>{name}<ArrowUpRight size={15}/></button>)}</div>}{q&&results.map((r,i)=><button className="search-result" key={i} onClick={()=>{navigate(r.to);setSearch(false)}}><span>{r.name}</span><small>{r.type}</small></button>)}{q&&!results.length&&<p>No results. Try a product name, country, or shipment ID.</p>}</Modal><Modal open={bell} onClose={()=>setBell(false)} title="Notifications"><div className="notification-list">{state.notifications.map(n=><div key={n.id}><Bell size={18}/><span><strong>{n.title}</strong><small>{new Date(n.at).toLocaleString()}</small></span>{!n.read&&<span className="new-dot"/>}</div>)}</div><Button variant="secondary" onClick={()=>void update(n=>n.notifications.forEach(x=>x.read=true),'Notifications read')}>Mark all read</Button></Modal></div>
}
