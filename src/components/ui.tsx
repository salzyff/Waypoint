import * as Dialog from '@radix-ui/react-dialog';import {X,ArrowRight,ArrowUpRight,CheckCircle2,AlertTriangle,CircleAlert,GitPullRequest,Loader2} from 'lucide-react';import type {ReactNode,ButtonHTMLAttributes} from 'react';import {Link} from 'react-router-dom';import type {Status} from '../types';
export function Button({children,className='',variant='primary',...props}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger'}){return <button className={`btn ${variant} ${className}`} {...props}>{children}</button>}
export function Modal({open,onClose,title,children,wide=false}:{open:boolean;onClose:()=>void;title:string;children:ReactNode;wide?:boolean}){return <Dialog.Root open={open} onOpenChange={v=>!v&&onClose()}><Dialog.Portal><Dialog.Overlay className="overlay"/><Dialog.Content className={'modal '+(wide?'wide':'')}><div className="modal-head"><Dialog.Title>{title}</Dialog.Title><Dialog.Close className="icon-btn" aria-label="Close"><X size={20}/></Dialog.Close></div><Dialog.Description className="sr-only">{title} details and actions</Dialog.Description>{children}</Dialog.Content></Dialog.Portal></Dialog.Root>}
export function Badge({status,children}:{status:Status;children?:ReactNode}){const Icon=status==='blocked'?CircleAlert:status==='review'?GitPullRequest:status==='warning'?AlertTriangle:CheckCircle2;return <span className={'badge '+status}><Icon size={13}/>{children??({ready:'Ready',warning:'Ready with warnings',blocked:'Blocked',review:'Human review',draft:'Draft'}[status])}</span>}
export function PageHead({eyebrow,title,description,children}:{eyebrow?:string;title:string;description?:string;children?:ReactNode}){return <div className="page-head"><div>{eyebrow&&<div className="eyebrow">{eyebrow}</div>}<h1>{title}</h1>{description&&<p>{description}</p>}</div><div className="head-actions">{children}</div></div>}
export function Empty({title,description,to,label='New shipment'}:{title:string;description:string;to?:string;label?:string}){return <div className="empty"><div className="empty-icon"><ArrowUpRight/></div><h3>{title}</h3><p>{description}</p>{to&&<Link className="btn primary" to={to}>{label}<ArrowRight size={16}/></Link>}</div>}
export function Loading(){return <div className="empty"><Loader2 className="spin"/><h3>Loading your workspace…</h3></div>}
export const Notice=()=> <p className="legal">WAYPOINT provides trade-preparation assistance. Official requirements should be verified with relevant authorities and licensed professionals before commercial shipment.</p>;
