import {lazy,Suspense,Component,type ReactNode} from 'react';import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {StoreProvider,useStore} from './store';import Shell from '../components/Shell';import {Loading,Button} from '../components/ui';import {demo} from '../services/backend';
const Landing=lazy(()=>import('../features/Landing'));const Dashboard=lazy(()=>import('../features/Dashboard'));const Compiler=lazy(()=>import('../features/Compiler'));const Shipments=lazy(()=>import('../features/Shipments'));const NewShipment=lazy(()=>import('../features/NewShipment'));const Simulator=lazy(()=>import('../features/Simulator'));const Passport=lazy(()=>import('../features/Passport'));const Compare=lazy(()=>import('../features/Compare'));const Onboarding=lazy(()=>import('../features/Onboarding'));const Auth=lazy(()=>import('../features/Auth'));const Products=lazy(()=>import('../features/WorkspaceData').then(m=>({default:m.Products})));const Documents=lazy(()=>import('../features/WorkspaceData').then(m=>({default:m.Documents})));const Organisation=lazy(()=>import('../features/WorkspaceData').then(m=>({default:m.Organisation})));const Settings=lazy(()=>import('../features/WorkspaceData').then(m=>({default:m.Settings})));const Rules=lazy(()=>import('../features/RuleExplorer').then(m=>({default:m.RuleExplorer})));const RulePacks=lazy(()=>import('../features/Rules').then(m=>({default:m.RulePacks})));const Sources=lazy(()=>import('../features/Rules').then(m=>({default:m.Sources})));const Audit=lazy(()=>import('../features/Rules').then(m=>({default:m.AuditLogs})));
class Boundary extends Component<{children:ReactNode},{error:string}>{state={error:''};static getDerivedStateFromError(e:Error){return{error:e.message}}render(){return this.state.error?<div className="empty"><h1>We couldn’t load this view.</h1><p>{this.state.error}</p><Button onClick={()=>location.reload()}>Reload workspace</Button></div>:this.props.children}}
function Protected(){const{user,loading,error,reload}=useStore();if(loading)return <Loading/>;if(!demo&&!user)return <Navigate to="/login" replace/>;if(error.startsWith('No organisation membership'))return <Onboarding/>;if(error)return <div className="empty"><h2>Workspace unavailable</h2><p>{error}</p><Button onClick={()=>void reload()}>Try again</Button></div>;return <Shell/>}
const queryClient=new QueryClient({defaultOptions:{queries:{staleTime:60000,retry:1,refetchOnWindowFocus:false}}});
export default function App(){return <Boundary><QueryClientProvider client={queryClient}><BrowserRouter><StoreProvider><Suspense fallback={<Loading/>}><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth/>}/><Route element={<Protected/>}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/shipments" element={<Shipments/>}/><Route path="/shipments/new" element={<NewShipment/>}/><Route path="/shipments/:id/edit" element={<NewShipment/>}/><Route path="/compile/:id" element={<Compiler/>}/><Route path="/compile" element={<Navigate to="/shipments"/>}/><Route path="/shipments/:id/passport" element={<Passport/>}/><Route path="/shipments/:id/simulate" element={<Simulator/>}/><Route path="/products" element={<Products/>}/><Route path="/compare" element={<Compare/>}/><Route path="/documents" element={<Documents/>}/><Route path="/rules" element={<Rules/>}/><Route path="/organisation" element={<Organisation/>}/><Route path="/settings" element={<Settings/>}/><Route path="/admin/rule-packs" element={<RulePacks/>}/><Route path="/admin/sources" element={<Sources/>}/><Route path="/admin/audit" element={<Audit/>}/><Route path="*" element={<div className="empty"><h1>This route isn’t in the preparation path.</h1><a className="btn primary" href="/dashboard">Back to overview</a></div>}/></Route></Routes></Suspense></StoreProvider></BrowserRouter></QueryClientProvider></Boundary>}
